# ============================================================================
# requirements-api.txt — VineGuard AI  |  FastAPI Backend
# ============================================================================
# Entorno: venv_api  |  Python 3.11
#
# INSTALACION EN DOS PASOS (obligatorio por conflicto de typing-extensions):
#
#   Paso 1 — todas las dependencias (incluyendo las de TF):
#     pip install -r requirements-api.txt
#
#   Paso 2 — TensorFlow SIN resolver sus deps (evita typing-extensions<4.6):
#     pip install tensorflow==2.13.0 --no-deps
#     pip install tensorflow-intel==2.13.0 --no-deps
#
# Por que --no-deps en TF:
#   TF 2.13 declara typing-extensions<4.6, pero Pydantic 2.5, FastAPI y
#   SQLAlchemy necesitan >=4.6. Con --no-deps pip omite esa restriccion;
#   en la practica typing-extensions 4.16 es compatible con TF 2.13.
#   Las deps de TF si se instalan normalmente desde este archivo.
#
# Nota: passlib eliminado. El proyecto usa bcrypt directo.
# ============================================================================

# --- Web framework ---
fastapi==0.110.0
uvicorn==0.27.0
python-multipart==0.0.9
httpx==0.27.0

# --- Auth / JWT ---
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# --- Validation ---
pydantic==2.5.0
pydantic-settings==2.1.0

# --- Database ---
sqlalchemy==2.0.23
alembic==1.14.0
aiosqlite==0.19.0

# --- ML runtime (required by prediction import chain) ---
# TensorFlow se instala SEPARADO con --no-deps (ver cabecera de este archivo)
# NO incluir tensorflow aqui para evitar el conflicto de typing-extensions
numpy==1.24.3
scikit-learn==1.3.0
scikit-image==0.21.0
scipy==1.11.3
pillow==10.0.1
matplotlib==3.7.2
joblib==1.3.2

# --- TensorFlow 2.13.0 dependencies (TF se instala con --no-deps, sus deps aqui) ---
absl-py>=1.0.0
astunparse>=1.6.0
flatbuffers>=23.1.21
gast>=0.2.1,<=0.4.0
google-pasta>=0.1.1
grpcio>=1.24.3,<2.0
h5py>=2.9.0
keras>=2.13.1,<2.14
libclang>=13.0.0
ml-dtypes==0.2.0
opt-einsum>=2.3.2
protobuf>=3.20.3,<5.0.0dev
setuptools
six>=1.12.0
termcolor>=1.1.0
wrapt>=1.11.0,<1.15
tensorflow-estimator>=2.13.0,<2.14
tensorboard>=2.13,<2.14

# --- Typing ---
typing-extensions==4.16.0

python-docx==1.1.0
openpyxl==3.1.2
reportlab==4.2.2